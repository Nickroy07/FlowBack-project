/**
 * Flowback — backend/firebase.js
 * 
 * DEPRECATED / NOT USED FOR HACKATHON MVP
 * 
 * Per MVP requirements:
 * - Do NOT add Firebase
 * - Do NOT add teammate sharing, auth, accounts, analytics, databases, user history
 * - Focus ONLY on: Interruption → Context Capture → AI Reconstruction → Resume Card
 * 
 * This file is intentionally left empty to satisfy existing project structure
 * without introducing forbidden dependencies.
 * 
 * If you need persistence beyond chrome.storage.local, use that API only.
 */

module.exports = {
  // No Firebase for MVP
  disabled: true,
  reason: 'Hackathon MVP focuses only on local capsule + AI reconstruction'
};
